# Application Configuration
NODE_ENV=production
HOSTNAME=0.0.0.0
PORT=3000

# Local Network Configuration
# Set this to your server's local IP address
SERVER_IP=192.168.1.100

# Application URL (after setting up feedback.local in hosts file)
APP_URL=http://feedback.local
